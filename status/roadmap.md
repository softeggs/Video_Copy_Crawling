# 路线图

## P0 基线固化

- 阶段目标：建立状态中心，固化真实现状，清理“旧后端仍可维护”的错误认知。
- 当前状态：已完成
- 已完成：
  - 建立 `status/` 文档体系
  - 明确后端重建、网页优先、iOS 暂缓
  - 建立长期模块分支：`backend/rebuild`、`core/pipeline-engine`、`frontend/web-app`、`ops/streamlit-console`、`ops/status-docs`、`app/ios-client`

## P1 后端可运行

- 阶段目标：让后端认证、视频、管理、健康检查、数据库调度全部可本地运行。
- 当前状态：已完成
- 已完成：
  - 重建 `backend/` 全部基础模块
  - 重建 `core.db_scheduler`
  - 通过 `test_backend_auth.py`
  - 通过 `test_backend_videos.py`
  - 通过 `test_backend_admin.py`
  - 通过 `test_db_scheduler.py`
  - 通过 `alembic upgrade head`

## P2 Web 用户端

- 阶段目标：交付与 iOS 同范围的网页端。
- 当前状态：开发中
- 已完成：
  - 建立 `React + Vite + TypeScript` 骨架
  - 实现登录、注册、提交、历史、详情、个人页基础页面
  - 对接统一后端接口
  - 通过 `npm test`
  - 通过 `npm run build`
  - 支持通过 `web_app/.env.local` 切换前端本地后端端口
  - 已补齐提交页 URL 校验
  - 已完成合法视频链接的手动联调验证，确认可成功提交并写库
- 未完成：
  - 增加更完整的交互测试
  - 打磨历史记录分页、状态筛选、空态、错误态和加载态
  - 继续完善详情页、历史页的体验与异常反馈

## P3 内部调试台

- 阶段目标：将 `app.py` 降级为内部运维与观测控制台。
- 当前状态：开发中
- 已完成：
  - 用全新调试台替换旧前台流程
  - 展示后端健康状态、数据库摘要、用户/视频列表
  - 支持手动触发数据库调度
  - 支持查看数据库明细
  - 已通过手动联调验证“调度 -> 转录 -> 写库 -> 飞书同步”链路
- 未完成：
  - 增加失败任务和错误详情展示
  - 增加更细的任务日志和 API 调用摘要

## P4 服务器迁移准备

- 阶段目标：在本地验证完成后，再迁移到服务器。
- 当前状态：待开始
- 当前任务：
  - 固化 PostgreSQL 目标配置
  - 补齐部署说明
  - 整理迁移清单

最后更新：2026-03-25
