# 核心流水线

## 模块定位

负责下载、音频处理、转写、AI 结构化和飞书同步，是数据库调度的执行引擎。

## 当前状态

已运行

## 已完成

- 现有 `core.pipeline` 可串联下载、ASR、AI、Markdown、飞书同步
- 已新增 `core.db_scheduler`，开始承接数据库驱动的任务认领与回写

## 未完成

- 将数据库调度与流水线做完整本地联测
- 增加面向内部调试台的运行摘要接口

## 验证证据

- `core/pipeline.py` 已存在完整 `process` 流程
- `core/db_scheduler.py` 已新增数据库认领、写回、失败恢复逻辑

## 当前难点

- 当前正式处理链路仍以原始流水线为中心，后续需要更清晰的“提交”和“执行”边界

## 下一步任务

- 用后端测试和手动任务触发验证数据库调度闭环

## 最后更新

2026-03-20
