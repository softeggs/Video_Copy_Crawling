# 网页前端

## 模块定位

新的正式用户端入口，与后端共用统一认证、视频、详情和个人页数据。

## 当前状态

开发中

## 已完成

- 建立 `React + Vite + TypeScript` 项目结构
- 实现登录、注册、提交、历史、详情、个人页基础页面
- 封装统一 API 客户端和会话存储
- 完成一次 `vitest` 最小测试
- 完成一次 `vite build` 构建验证
- 已支持通过 `web_app/.env.local` 切换前端本地后端端口
- 已定位前端默认连到 `8001`、实际新后端运行在 `8002` 的联调问题
- 提交页已增加 URL 校验
- 已完成提交页 URL 校验与合法链接提交流程手测
- 已确认前端在 `npm run dev` 模式下可联通 `8002` 后端并成功写库

## 未完成

- 增加更多单测和交互测试
- 增加历史记录分页和状态筛选
- 细化错误态、空态和加载态
- 后续补充管理端页面
- 补强历史页、详情页和失败场景的体验细节

## 验证证据

- `npm test`
- `npm run build`
- `web_app/.env.local` 已支持配置本地后端端口

## 当前难点

- 当前只是第一版用户端，尚未扩展为完整产品体验
- 本地联调时必须确保前端实际指向 `8002` 新后端，而不是旧服务 `8001`

## 下一步任务

- 再补列表筛选、分页、错误态与测试覆盖
- 增加对失败记录与异常平台的可视化提示

## 最后更新

2026-03-25
